% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loading_data.R
\name{loadAll}
\alias{loadAll}
\title{Load Multiple Data Files into a Named List}
\usage{
loadAll(
  file_dir = "./",
  file_pattern = "*.csv",
  header = TRUE,
  sep = ",",
  skip = 0,
  encoding = "UTF-8",
  clean_names = TRUE,
  na.strings = c("NA", "", "NULL"),
  verbose = TRUE
)
}
\arguments{
\item{file_dir}{Character string specifying the directory containing input files (default: "./")}

\item{file_pattern}{Character string specifying the file pattern to match (default: "*.csv")}

\item{header}{Logical indicating if files have headers (default: TRUE)}

\item{sep}{Character specifying the field separator in files (default: ",")}

\item{skip}{Numeric indicating number of lines to skip before reading data (default: 0)}

\item{encoding}{Character string specifying file encoding (default: "UTF-8")}

\item{clean_names}{Logical indicating whether to clean column names (default: TRUE)}

\item{na.strings}{Character vector of strings to be interpreted as NA values (default: c("NA", "", "NULL"))}

\item{verbose}{Logical indicating whether to show progress and information (default: TRUE)}
}
\value{
A named list of data frames, where names are derived from source filenames
}
\description{
Reads multiple files from a directory matching a pattern and returns them as a named
list where each element is a data frame named after its source file (without extension).
}
\details{
The function performs the following operations:
\itemize{
\item Validates input parameters and file accessibility
\item Reads all matching files from the specified directory
\item Creates a named list of data frames
\item Optionally cleans column names for compatibility
\item Provides progress feedback if verbose is TRUE
}
}
\note{
\itemize{
\item Handles missing or corrupted files gracefully
\item Preserves original column types where possible
\item Supports various file formats through pattern matching
\item Names are sanitized for R compatibility
}
}
\examples{
\dontrun{
# Basic usage
data_list <- loadAll()

# Specific directory and pattern
data_list <- loadAll(
  file_dir = "path/to/files",
  file_pattern = "*.tsv",
  sep = "\t"
)

# Access individual datasets
first_dataset <- data_list[[1]]
named_dataset <- data_list[["filename"]]
}

}
