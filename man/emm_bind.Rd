% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emmeans-utils.R
\name{emm_bind}
\alias{emm_bind}
\title{Bind Multiple Estimated Marginal Means into a Table}
\usage{
emm_bind(
  emm,
  digits = 2,
  format = "markdown",
  caption = NULL,
  col.names = NULL,
  kable = TRUE
)
}
\arguments{
\item{emm}{List of emmeans objects. Names of the list elements are used as model identifiers.}

\item{digits}{Integer. Number of decimal places for numeric columns (default: 2)}

\item{format}{Character. Output format: "markdown", "html", or "latex" (default: "markdown")}

\item{caption}{Character. Optional table caption}

\item{col.names}{Character vector. Custom column names}
}
\value{
A knitr_kable object that can be directly used in R Markdown or Quarto
}
\description{
Combines multiple emmeans objects into a single formatted table, with model names
as identifiers. The output is compatible with knitr/quarto rendering.
}
\examples{
\dontrun{
# Create multiple emmeans objects
emm1 <- emmeans(model1, ~treatment)
emm2 <- emmeans(model2, ~treatment)

# Combine into table
emm_table <- emm_bind(
  list("Model 1" = emm1, "Model 2" = emm2),
  digits = 3,
  caption = "Estimated Marginal Means Comparison"
)

# Print table
emm_table
}

}
