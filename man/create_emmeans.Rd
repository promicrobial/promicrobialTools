% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emmeans-utils.R
\name{create_emmeans}
\alias{create_emmeans}
\title{Create Standardized EMMs with Flexible Specifications}
\usage{
create_emmeans(
  model,
  specs,
  at_values = NULL,
  adjust_method = "sidak",
  weights = "proportional",
  type = "response",
  component = "cond",
  transform = NULL,
  level_labels = NULL
)
}
\arguments{
\item{model}{A fitted model object (e.g., from glmmTMB, lme4, etc.)}

\item{specs}{Formula or character string specifying the emmeans structure}

\item{at_values}{Named list of values at which to evaluate emmeans (default: NULL)}

\item{adjust_method}{Character string specifying p-value adjustment method (default: "sidak" (the same as the in the emmeans package))}

\item{weights}{Character string specifying weighting method (default: "proportional")}

\item{type}{Character string specifying the scale for emmeans (default: "response")}

\item{component}{Character string specifying model component for zero-inflated models (default: "cond")}

\item{transform}{Character string specifying transformation to apply (default: NULL)}

\item{level_labels}{Named list of custom labels for factor levels (default: NULL)}
}
\value{
An emmGrid object
}
\description{
This function creates emmeans objects with flexible specifications for different
study designs. It handles custom transformations, level labeling, and various
emmeans parameters.
}
\details{
The function creates emmeans objects with consistent parameter handling across
different study designs. Custom level labels can be applied to make results
more interpretable.
}
\examples{
warp.lm <- lm(breaks ~ wool * tension, data = warpbreaks)

# Simple comparison
create_emmeans(
  model = warp.lm,
  specs = ~ wool | tension,
  by = "tension",
  level_labels = list(tension = c("Low", "Medium", "High"))
)
\dontrun{

# Complex interaction with zero-inflated component
emm_zi <- create_emmeans(
  model = zi_model,
  specs = ~ treatment | time,
  component = "zi",
  transform = "logit"
)
}

}
\seealso{
\code{\link[emmeans]{emmeans}}, \code{\link[emmeans]{regrid}}
}
