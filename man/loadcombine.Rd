% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loading_data.R
\name{loadcombine}
\alias{loadcombine}
\title{Load and Combine Multiple Data Files}
\usage{
loadcombine(
  file_dir,
  file_pattern,
  header = TRUE,
  sep = "\\t",
  skip = 0,
  output,
  clean_names = TRUE,
  encoding = "UTF-8",
  na.strings = c("NA", "", "NULL")
)
}
\arguments{
\item{file_dir}{Character string specifying the directory containing input files}

\item{file_pattern}{Character string specifying the file pattern to match (e.g., "*.tsv")}

\item{header}{Logical indicating if files have headers (default: TRUE)}

\item{sep}{Character specifying the field separator in files (default: "\t")}

\item{skip}{Numeric indicating number of lines to skip before reading data (default: 0)}

\item{output}{Character string specifying the path for the output file}

\item{clean_names}{Logical indicating whether to clean column names (default: TRUE)}

\item{encoding}{Character string specifying file encoding (default: "UTF-8")}

\item{na.strings}{Character vector of strings to be interpreted as NA values (default: c("NA", "", "NULL"))}
}
\value{
Invisibly returns the combined data frame
}
\description{
Reads multiple files from a directory matching a pattern, combines them into a single
data frame, and exports the result. Each file's content is labeled with its source
filename in a 'sample' column.
}
\details{
The function performs the following operations:
\itemize{
\item Validates input parameters and file accessibility
\item Reads all matching files from the specified directory
\item Combines files into a single data frame with a 'sample' column indicating source
\item Optionally cleans column names for compatibility
\item Exports the combined data to the specified output file
}
}
\note{
\itemize{
\item Requires data.table and dplyr packages
\item Handles missing columns across files using data.table's fill parameter
\item Preserves the original working directory
\item Attempts to standardize column types across files
}
}
\examples{
\dontrun{
loadcombine(
  file_dir = "path/to/files",
  file_pattern = "*.tsv",
  sep = "\t",
  output = "path/to/output.csv",
  clean_names = TRUE
)
}

}
