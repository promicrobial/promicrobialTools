% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_data.R
\name{process_data}
\alias{process_data}
\title{Process Wide-Format Rating Data to Long Format}
\usage{
process_data(data, group_var = NULL)
}
\arguments{
\item{data}{A data frame containing paired x-y ratings in wide format.
Column names should follow the pattern 'x1', 'y1', 'x2', 'y2', etc.}

\item{group_var}{Optional. The name of the grouping variable column (unquoted).
If NULL (default), no grouping is preserved.}
}
\value{
A tibble in long format with columns:
- x: x-values from the original pairs
- y: y-values from the original pairs
- reviewer: Reviewer ID in the format "R1", "R2", etc.
- group: (if group_var is specified) The grouping variable values
}
\description{
Transforms rating data from wide format (with multiple x-y pairs) to long format,
optionally preserving a grouping variable. The function handles paired x-y ratings
from multiple reviewers and creates a standardized reviewer ID.
}
\examples{
\dontrun{
# Without grouping
result1 <- process_data(data)

# With grouping
result2 <- process_data(data, group)
}

}
