% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary-stats.r
\name{data_summary}
\alias{data_summary}
\title{Calculate Summary Statistics for Numeric Data}
\usage{
data_summary(data, varname = NULL, groupnames = NULL, na.rm = TRUE, digits = 3)
}
\arguments{
\item{data}{A numeric vector, matrix, or data frame}

\item{varname}{Character string specifying the variable name to summarize.
If NULL and data is a vector, uses the entire vector.}

\item{groupnames}{Character vector of column names to group by.
Optional for data frames, ignored for vectors and matrices.}

\item{na.rm}{Logical, whether to remove NA values from grouping variables (default = TRUE)}

\item{digits}{Number of decimal places for rounding (default = 3)}
}
\value{
A data frame containing summary statistics:
\itemize{
\item n (sample size)
\item mean
\item sd (standard deviation)
\item SEM (standard error of the mean)
\item median
\item max (maximum value)
\item min (minimum value)
}
}
\description{
Calculates summary statistics (mean, standard deviation, standard error,
median, maximum, and minimum) for numeric data. Works with vectors, matrices, or
data frames, with optional grouping variables.
}
\examples{
# Vector example
vec <- rnorm(100)
data_summary(vec)

# Data frame example with grouping
df <- data.frame(
  value = rnorm(100),
  group = rep(c("A", "B"), each = 50)
)
data_summary(df, "value", "group")

}
