% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delim.R
\name{delim}
\alias{delim}
\title{Convert Data Frame to Delimited Text Format}
\usage{
delim(
  x,
  format = c("csv", "tsv", "custom"),
  sep = ",",
  row.names = FALSE,
  col.names = TRUE,
  quote = TRUE,
  na = "",
  trim = TRUE,
  scientific = FALSE,
  digits = NULL
)
}
\arguments{
\item{x}{A data frame or matrix to convert}

\item{format}{Output format, one of "csv", "tsv", or "custom" (default: "csv")}

\item{sep}{Custom separator to use when format="custom" (default: ",")}

\item{row.names}{Logical indicating whether to include row names (default: FALSE)}

\item{col.names}{Logical indicating whether to include column names (default: TRUE)}

\item{quote}{Logical indicating whether to quote strings (default: TRUE)}

\item{na}{String to use for NA values (default: "")}

\item{trim}{Logical indicating whether to trim whitespace from values (default: TRUE)}

\item{scientific}{Logical indicating whether to use scientific notation (default: FALSE)}

\item{digits}{Integer indicating number of decimal places for numeric values (default: NULL)}
}
\value{
A character string containing the formatted data
}
\description{
Converts a data frame or matrix to a delimited text format (CSV, TSV, or custom separator).
Similar to print() or kable() but returns a string formatted for direct writing to file
or displaying in console with chosen delimiter.
}
\examples{
# Create example data frame
df <- data.frame(
  A = c(1, 2, 3),
  B = c("a", "b", "c"),
  C = c(1.234, 5.678, 9.012)
)

# CSV format
delim(df)

# TSV format
delim(df, format = "tsv")

# Custom separator
delim(df, format = "custom", sep = "|")

# With row names and 2 decimal places
delim(df, row.names = TRUE, digits = 2)
}
