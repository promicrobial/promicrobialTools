% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_values.r
\name{scale_values}
\alias{scale_values}
\title{Scale Numeric Values to \link{0,1} Range}
\usage{
scale_values(x, na.rm = TRUE, preserve_zero = FALSE, tolerance = 1e-10)
}
\arguments{
\item{x}{Numeric vector, matrix, or data frame to be scaled}

\item{na.rm}{Logical. Should missing values be removed when computing min/max? (default: TRUE)}

\item{preserve_zero}{Logical. Should zero values remain zero after scaling? (default: FALSE)}

\item{tolerance}{Numeric. Values below this threshold are considered zero (default: 1e-10)}
}
\value{
An object of the same type and dimensions as the input, with values scaled to \link{0,1}.
Returns NA for constant vectors (where min = max).
}
\description{
Scales numeric values to the \link{0,1} range using min-max normalization. The function
handles missing values, infinite values, and edge cases while preserving the relative
distribution of the data.
}
\details{
The function applies the following transformation:
\deqn{x_{scaled} = \frac{x - min(x)}{max(x) - min(x)}}

Special cases:
\itemize{
\item If all values are identical, returns NA with a warning
\item Handles infinite values by converting them to NA
\item Can preserve zero values if preserve_zero = TRUE
}
}
\examples{
# Simple vector scaling
x <- c(1, 2, 3, 4, 5)
scale_values(x)

# Handling missing values
x_na <- c(1, 2, NA, 4, 5)
scale_values(x_na, na.rm = TRUE)

# Preserving zeros
x_zeros <- c(0, 1, 2, 3, 0)
scale_values(x_zeros, preserve_zero = TRUE)

# Scaling a matrix
mat <- matrix(1:9, nrow = 3)
scale_values(mat)

}
\seealso{
\code{\link[https://cran.r-project.org/doc/manuals/r-release/fullrefman.pdf#Rfn.scale.1]{scale}}, \code[https://cran.r-project.org/doc/manuals/r-release/fullrefman.pdf#Rfn.sweep.1]{\link{sweep}}
}
