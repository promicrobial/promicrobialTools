% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparsity-utils.R
\name{randspar}
\alias{randspar}
\title{Generate Random Sparse Matrix or Vector}
\usage{
randspar(n, min = 0, max = 1, d = 10, mat = TRUE, nrow = NULL)
}
\arguments{
\item{n}{numeric. Total number of observations to generate}

\item{min}{numeric. Minimum value for random numbers (default: 0)}

\item{max}{numeric. Maximum value for random numbers (default: 1)}

\item{d}{numeric. Desired density as a proportion (e.g., 10 means 10\% non-zero values) (default: 10)}

\item{mat}{logical. If TRUE returns a matrix, if FALSE returns a vector (default: TRUE)}

\item{nrow}{numeric. Number of rows for matrix output. If NULL, defaults to 10 (default: NULL)}
}
\value{
If mat=TRUE, returns a matrix with dimensions nrow x (n/nrow).
If mat=FALSE, returns a vector of length n. Both contain random values between
min and max, with d\% of elements being nonzero.
}
\description{
Creates a random sparse matrix or vector with user-specified density
and value range. Values are uniformly distributed between min and max, with zeros
inserted to achieve the desired sparsity.
}
\examples{
# Generate a sparse vector with 100 elements, 10\% nonzeros
randspar(n = 100, mat = FALSE)

# Generate a 5x20 sparse matrix with 20\% nonzeros
randspar(n = 100, d = 20, nrow = 5)

# Generate a sparse matrix with values between -1 and 1
randspar(n = 100, min = -1, max = 1)

}
