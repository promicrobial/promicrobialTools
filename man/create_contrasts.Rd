% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emmeans-utils.R
\name{create_contrasts}
\alias{create_contrasts}
\title{Create Flexible Contrasts for Different Study Designs}
\usage{
create_contrasts(
  emm_object,
  contrast_type = "pairwise",
  by_vars = NULL,
  reverse = TRUE,
  custom_contrasts = NULL,
  adjust_method = "sidak"
)
}
\arguments{
\item{emm_object}{An emmGrid object from emmeans}

\item{contrast_type}{Character string specifying contrast type: "pairwise",
"sequential", "vs_control", "vs_reference", or "custom" (default: "pairwise")}

\item{by_vars}{Character vector of variables to group contrasts by (default: NULL)}

\item{reverse}{Logical indicating whether to reverse contrast direction (default: TRUE)}

\item{custom_contrasts}{Custom contrast matrix or list for custom contrasts (default: NULL)}

\item{adjust_method}{Character string specifying p-value adjustment method (default: "sidak")}

\item{method}{Character string specifying contrast method (default: "revpairwise")}
}
\value{
A contrast object (emmGrid)
}
\description{
This function creates various types of contrasts from emmeans objects,
supporting different contrast types commonly used in research.
}
\details{
The function supports multiple contrast types:
\itemize{
\item \code{pairwise}: All pairwise comparisons
\item \code{sequential}: Sequential/consecutive comparisons
\item \code{vs_control}: Compare all levels to control (first level)
\item \code{vs_reference}: Compare all levels to reference (Dunnett's test)
\item \code{custom}: Use custom contrast matrix
}
}
\examples{
warp.lm <- lm(breaks ~ wool * tension, data = warpbreaks)

# Simple comparison
emm <- create_emmeans(
  model = warp.lm,
  specs = ~ wool | tension,
  level_labels = list(tension = c("Low", "Medium", "High"))
)
\dontrun{
# Simple pairwise contrasts
contrasts <- create_contrasts(emm, contrast_type = "pairwise")

# Treatment contrasts by time
contrasts <- create_contrasts(
  emm, 
  contrast_type = "pairwise",
  by_vars = "time"
)

# Custom contrasts
custom_matrix <- list(
  "Treatment_vs_Control" = c(-1, 1, 0, 0),
  "Time_effect" = c(-1, 0, 1, 0)
)
contrasts <- create_contrasts(
  emm,
  contrast_type = "custom",
  custom_contrasts = custom_matrix
)
}

}
\seealso{
\code{\link[emmeans]{contrast}}, \code{\link[emmeans]{pairs}}
}
