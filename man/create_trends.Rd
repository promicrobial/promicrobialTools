% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emmeans-utils.R
\name{create_trends}
\alias{create_trends}
\title{Create Trends (Slopes) with Flexible Specifications and perform pairwise comparisons thereof}
\usage{
create_trends(
  model,
  trend_var,
  specs = NULL,
  at_values = NULL,
  adjust_method = "sidak",
  level_labels = NULL
)
}
\arguments{
\item{model}{A fitted model object}

\item{trend_var}{Character string specifying the variable to compute trends for}

\item{specs}{Formula or character string specifying the trend structure (default: NULL)}

\item{at_values}{Named list of values at which to evaluate trends (default: NULL)}

\item{adjust_method}{Character string specifying p-value adjustment method (default: "sidak")}

\item{level_labels}{Named list of custom labels for factor levels (default: NULL)}
}
\value{
An emtrends object (emmGrid)
}
\description{
This function creates emtrends objects for analyzing slopes/trends in
continuous variables with flexible grouping and labeling options.
}
\details{
This function wraps emtrends to provide consistent parameter handling and
custom labeling capabilities. When specs is NULL, it defaults to computing
trends across all observations.
}
\examples{
\dontrun{
fiber.lm <- lm(strength ~ diameter*machine, data=fiber)
# Simple time trends
trends <- create_trends(
  fiber.lm, 
  trend_var = "diameter"
  specs = ~ machine
)

# Treatment-specific time trends
trends <- create_trends(
  model,
  trend_var = "time",
  specs = ~ treatment,
  level_labels = list(treatment = c("Control", "Intervention"))
)

# Age-specific trends at specific time points
trends <- create_trends(
  model,
  trend_var = "time",
  specs = ~ age_group,
  at_values = list(age_group = c("young", "old"))
)
}

}
\seealso{
\code{\link[emmeans]{emtrends}}
}
